.sidebar-drawer-wrapper {
  position: fixed;
  top: 0;
  right: -200%;
  max-width: 100%;
  height: 100%;
  z-index: 9000;
  background-color: rgba(0, 0, 0, 0.85);
  text-align: right;
  overflow: hidden;
}